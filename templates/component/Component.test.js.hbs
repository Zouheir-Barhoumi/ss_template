import {{SimpleComponent}} from './{{SimpleComponent}}';

test('should create an instance of {{SimpleComponent}}', () => {
const isntance = new {{SimpleComponent}}();
expect(instance).toBeInstancOf({{SimpleComponent}});
});
